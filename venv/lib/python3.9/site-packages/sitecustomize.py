
import platform
import sys

# 只在主要 PyInstaller 进程和相关子进程中生效
def patched_mac_ver(release='', versioninfo=('', '', ''), machine=''):
    return ('13.0', ('', '', ''), 'arm64')

try:
    platform.mac_ver = patched_mac_ver
    # 也可以尝试打印确认
    # print("🔥 Monkey-patching platform.mac_ver to 13.0", file=sys.stderr)
except Exception as e:
    pass
